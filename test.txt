html { <-- add HTML tag to slot 0

    body{ <-- add BODY tag to slot 1

    } <-- add the closing tags in REVERSE. BODY was the last tag added, so close the BODY tag!

} <--- add the FIRST closing tag, which would be HTML

outputs to

<html><body></body></html>